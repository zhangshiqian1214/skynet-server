local skynet = require "skynet"
local role_base = require "common.role_base"

local agent_ctrl = {}

function agent_ctrl.get_role(role_id)

end

function agent_ctrl.login_role(role_id)

end

function agent_ctrl.logout_role(role_id)

end

return agent_ctrl